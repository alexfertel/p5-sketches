///<reference path="../../p5.global-mode.d.ts" />

let width, height;

function setup() {
  width = windowWidth;
  height = windowHeight;
  createCanvas(width, height);
  background(1);
  colorMode(HSB, 255);

  // for(let i = 0; i < )

  
}

function draw() {


  
}

// function drawSquare(x, y, w, h) {
//   if 
// }
